<div class="p-user-profile__form" id="user_profile">
  <notifications group="information"></notifications>
  <div class="container">
    <div class="row">
      <div class="col-10 col-sm-9 col-lg-5 col-xl-4 mx-auto">
        <div class="p-user-profile__form__wrapper">
          <div class="text-center">
            <p>画像と名前を登録しましょう！</p>
          </div>
          <div class="col-12 text-center">
            <label for="file_upload">
              <img :src="avatar" class="p-user-profile__form__avatar">
              <input name="uploadedImage" class="mt-3 d-none" type="file" ref="file" accept="image/*" @change="selectImage" id="file_upload">
            </label>
          </div>
          <div class="col-12">
            <label>ニックネーム</label>
          </div>
          <div class="col-12">
            <input v-model="user_profile.nickname" class="form-control" @keyup="inputNickname">
            <div v-if="user_profile.nickname.length > 0">
              <p><i class="fas fa-check-circle fa-sm" :class="[ nicknameStatus === true ? 'text-success' : 'text-danger' ]"></i> ８文字以内</p>
            </div>
            <div v-else>
              <p class="text-danger p-user-profile__form__notice-text">＊ニックネームを入力してください</p>
            </div>
          </div>
          <div class="col-12 text-center mt-3">
            <button class="btn p-user-profile__form__submit-btn" @click="submit" enctype="multipart/form-data">
              登録する
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%= javascript_packs_with_chunks_tag 'user/user_profile_new' %>